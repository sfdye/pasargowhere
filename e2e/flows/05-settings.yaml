appId: ${APP_ID}
tags: [smoke]
---
# Settings: map-provider row check (iOS), reminders toggle, language toggle en→zh→en.
- runFlow: ../utils/launch.yaml
- tapOn:
    id: tab-mypasars
- waitForAnimationToEnd
# Dismiss auto-opened add modal if present.
- runFlow:
    when:
      visible: { id: picker-row }
    commands:
      - tapOn: "Search.*"
      - inputText: Tiong Bahru
      - tapOn: "Tiong Bahru Market"
      - runFlow:
          when:
            platform: iOS
          commands:
            - tapOn: "close"
            - waitForAnimationToEnd
      - runFlow:
          when:
            platform: Android
          commands:
            - pressKey: Back
            - waitForAnimationToEnd
      - runFlow:
          when:
            platform: iOS
          commands:
            - tapOn:
                id: done-button
            - waitForAnimationToEnd
      - runFlow:
          when:
            platform: Android
          commands:
            - tapOn: "Navigate up"
            - waitForAnimationToEnd
# Navigate to Settings.
- tapOn:
    id: tab-settings
- waitForAnimationToEnd
# If on iOS, check the Apple Maps provider row on the maps sub-page (do this first,
# before toggling reminders scrolls the page down).
- runFlow:
    when:
      platform: iOS
    commands:
      - tapOn:
          id: row-maps
      - waitForAnimationToEnd
      - assertVisible:
          id: map-provider-apple
      - tapOn:
          id: tab-settings
      - waitForAnimationToEnd
# Toggle reminders on (will trigger a notification permission prompt on first enable).
- tapOn:
    id: settings-reminders
- waitForAnimationToEnd
# Dismiss notification permission dialog if it appears (iOS uses curly apostrophe).
- runFlow:
    when:
      visible: "Allow"
    commands:
      - tapOn: "Don.*Allow"
- waitForAnimationToEnd
# Toggle reminders off.
- tapOn:
    id: settings-reminders
- waitForAnimationToEnd
# Switch language to Chinese on the language sub-page (last — no need to scroll back).
- tapOn:
    id: row-language
- waitForAnimationToEnd
- tapOn:
    id: lang-zh
- waitForAnimationToEnd
# Verify language switch by checking a visible Chinese string.
- assertVisible: "语言"
# Switch back to English.
- tapOn:
    id: lang-en
- waitForAnimationToEnd
- assertVisible: "Language"
# Navigate back to Today.
- tapOn:
    id: tab-mypasars
