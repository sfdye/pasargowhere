appId: ${APP_ID}
tags: [smoke]
---
# Favorites: swipe-to-delete a market from the My Pasars list.
# Precondition: at least one favourite exists.
- runFlow: ../utils/launch.yaml
- tapOn:
    id: tab-mypasars
- waitForAnimationToEnd
# If no favourites exist, add one via the picker.
- runFlow:
    when:
      notVisible: { id: market-row }
    commands:
      - tapOn:
          id: add-markets
      - waitForAnimationToEnd
      - tapOn: "Search.*"
      - inputText: Tiong Bahru
      - tapOn: "Tiong Bahru Market"
      - runFlow:
          when:
            platform: iOS
          commands:
            - tapOn: "close"
            - waitForAnimationToEnd
      - runFlow:
          when:
            platform: Android
          commands:
            - pressKey: Back
            - waitForAnimationToEnd
      - runFlow:
          when:
            platform: iOS
          commands:
            - tapOn:
                id: done-button
            - waitForAnimationToEnd
      - runFlow:
          when:
            platform: Android
          commands:
            - tapOn: "Navigate up"
            - waitForAnimationToEnd
# Dismiss the reminder prompt card if present (it can overlap with the row).
- runFlow:
    when:
      visible: "Not now"
    commands:
      - tapOn: "Not now"
- waitForAnimationToEnd
# Swipe left on the first market row to reveal delete.
# Row position differs by platform: iOS ~31%, Android ~23%.
- runFlow:
    when:
      platform: iOS
    commands:
      - swipe:
          start: 90%, 31%
          end: 10%, 31%
- runFlow:
    when:
      platform: Android
    commands:
      - swipe:
          start: 90%, 23%
          end: 10%, 23%
- waitForAnimationToEnd
- tapOn:
    id: delete-favorite
- waitForAnimationToEnd
# The market row should be gone.
- assertNotVisible:
    id: market-row
