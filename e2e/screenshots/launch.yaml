appId: ${APP_ID}
---
# Screenshot-only launch: grants all permissions so Discover shows the
# "Near you" rail. The npm scripts (screenshots:ios / screenshots:android)
# set the simulator/emulator location to Singapore city centre before
# running Maestro, so no manual setup is needed.
- launchApp:
    clearState: true
    permissions: { all: allow }
